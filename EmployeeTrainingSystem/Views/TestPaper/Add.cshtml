@model IEnumerable<EmployeeTrainingEntity.Teacher>
    @using EmployeeTrainingEntity
    @{
    ViewData["Title"] = "Add";
    Layout = "~/Views/Shared/_Layout.cshtml";
    }
    @using (Html.BeginForm("Add", "TestPaper", FormMethod.Post))
    {
    <fieldset align="center">
        <legend>新建试卷</legend>
        <div align="center">
            <table>
                <tr>
                    <td>试卷名称：</td>
                    <td>
                        <input type="text" name="Name" />
                    </td>
                </tr>
                @*<tr>
                    <td>老师：</td>
                    <td>
                        <select style="width:100px;" name="Teacher.ID">
                            @foreach (var item in ViewBag.TeacherValue)
                            {
                                <option value="@item.ID">@item.Name</option>
                            }
                        </select>
                    </td>
                </tr>
                <tr>
                    <td>班级：</td>
                    <td>
                        <select style="width:100px;" name="CollegeClass.ID">
                            @foreach (var item in ViewBag.CollegeClassValue)
                            {
                                <option value="@item.ID">@item.Name</option>
                            }
                        </select>
                    </td>
                </tr>*@
                <tr>
                    <td>试卷创建时间：</td>
                    <td>
                        <input type="datetime-local" name="Time" />
                    </td>
                </tr>
                <tr>
                    <td>考试时间：</td>
                    <td>
                        <input type="text" name="TestTime" />
                    </td>
                </tr>
                <tr>
                    <td>成绩：</td>
                    <td>
                        <input type="text" name="Performance" />
                    </td>
                </tr>

            </table>
        </div>
    </fieldset>
    <input align="center" type="submit" class="btn btn-default" value="添加" style="margin-top: 20px; color:green;border:1px solid ;" />
    <div id="divContainer">

        <button type="button" class="btn btn-danger">@Html.ActionLink("返回", "Index", "TestPaper")</button>

    </div>
    }

